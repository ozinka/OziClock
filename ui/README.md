# Slint UI

Reusable Slint components and design tokens will live here after the Rust/Slint prototype gate is passed. Keep application rules in Rust crates; this directory owns declarative composition, rendering, and user-gesture callbacks only.
