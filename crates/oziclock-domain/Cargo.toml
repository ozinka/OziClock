[package]
name = "oziclock-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1.0.229", features = ["derive"], optional = true }
